/*
multiplicar(a, b)
Crear una función multiplicar que tome como argumentos dos números y devuelva la multiplicación de ellos

multiplicar(2, 3) // 6
multiplicar(4, 0.5) // 2
*/

const multiplicar = (multiplicarUno, multiplicarDos) => {
    return multiplicarUno * multiplicarDos;
}

alert (multiplicar(2, 3));
alert (multiplicar(4, 0.5));

